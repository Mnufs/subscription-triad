interface:
  display_name: "Subscription Triad"
  short_description: "Plan, review, and build through subscription CLIs"
  default_prompt: "Use $subscription-triad to plan, review, execute, and verify this feature."
