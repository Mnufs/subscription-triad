interface:
  display_name: "Subscription Triad"
  short_description: "Zero-config plan review and execution via subscription CLIs"
  default_prompt: "Use $subscription-triad to plan, review, execute, and verify this feature without changing Codex network defaults."
