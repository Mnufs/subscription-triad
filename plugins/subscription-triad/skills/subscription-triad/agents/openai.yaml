interface:
  display_name: "Subscription Triad"
  short_description: "One-authorization triad via subscription CLIs"
  default_prompt: "Use $subscription-triad to plan, review, execute, and verify this feature with one temporary provider authorization."
